[package]
name    = "engine"
version = "0.1.0"
edition = "2021"

[dependencies]
rayon     = { workspace = true }
thiserror = { workspace = true }

[[bench]]
name    = "compression"
harness = false

[dev-dependencies]
criterion = { workspace = true }
